

<h1>todays recommendations</h1>

{{#each accounts}}
  <section class='accounts'>
   <div>
    <h1>{{firstName}}</h1>
    <h2>{{lastName}}</h2>
    <p>{{age}}</p>
    <p>{{location}}away</p>
  <p><a href="/userprofile"{{accounts.id}}>"Meer info</a></p>
  </div>
  </section>
  
{{/each}}

